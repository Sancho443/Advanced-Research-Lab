# pytest.ini  ← create this file in root
[pytest]
testpaths = tests
python_paths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    network: marks tests as requiring internet access (deselect with '-m "not network"')